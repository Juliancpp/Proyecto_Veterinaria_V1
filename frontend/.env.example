# API base URL for the backend
# - With Nginx reverse proxy: use /api (recommended for production)
# - Without proxy (direct backend access): use full URL like https://demotb.virtusproject.com/api
VITE_API_URL=/api
